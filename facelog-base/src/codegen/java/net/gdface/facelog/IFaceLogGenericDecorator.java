// ______________________________________________________
// Generated by codegen - https://gitee.com/l0km/codegen 
// template: decorator/decorator.generic.class.vm
// ______________________________________________________
package net.gdface.facelog;
import java.io.IOException;
import java.util.List;
import java.util.Map;
import net.gdface.facelog.db.DeviceBean;
import net.gdface.facelog.db.DeviceGroupBean;
import net.gdface.facelog.db.FaceBean;
import net.gdface.facelog.db.FeatureBean;
import net.gdface.facelog.db.ImageBean;
import net.gdface.facelog.db.LogBean;
import net.gdface.facelog.db.PermitBean;
import net.gdface.facelog.db.PersonBean;
import net.gdface.facelog.db.PersonGroupBean;
import net.gdface.utils.FaceUtilits;
/**
 * decorator pattern 装饰者模式实现基于{@link IFaceLogDecorator}的支持泛型方法参数{@link IFaceLog}接口<br>
 * 所有 byte[] 类型的参数都支持泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}<br>
 * 注意:<br>
 * 参数为{@link java.io.InputStream}的方法返回时会自动执行{@link java.io.InputStream#close()}关闭流<br>
 * 参见 {@link net.gdface.utils.FaceUtilits#readBytes(java.io.InputStream)}<br>
 * unchecked后缀的方法将所有显式申明的异常封装到{@link RuntimeException}抛出<br>
 * 计算机生成代码(generated by automated tools DecoratorGenerator @author guyadong)<br>
 * @author guyadong
 *
 */
public class IFaceLogGenericDecorator extends IFaceLogDecorator{
    public IFaceLogGenericDecorator(IFaceLog delegate) {
        super(delegate);
    }
    //1
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,boolean,byte[],FaceBean,java.lang.String,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param personId personId
     * @param asIdPhotoIfAbsent asIdPhotoIfAbsent
     * @param featurePhoto featurePhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param faceBean faceBean
     * @param removed removed
     * @param token token
     * @return FeatureBean
     * @throws DuplicateRecordException DuplicateRecordException
     * @throws IOException IOException
     * @see IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,boolean,byte[],FaceBean,java.lang.String,Token)
     */    
    public <T> FeatureBean addFeature (T feature,String featureVersion,Integer personId,boolean asIdPhotoIfAbsent,T featurePhoto,FaceBean faceBean,String removed,Token token) throws DuplicateRecordException,IOException{
        return addFeature(FaceUtilits.getBytesNotEmpty(feature),featureVersion,personId,asIdPhotoIfAbsent,FaceUtilits.getBytesNotEmpty(featurePhoto),faceBean,removed,token);
    }
    //2
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,boolean,byte[],FaceBean,java.lang.String,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param feature feature
     * @param featureVersion featureVersion
     * @param personId personId
     * @param asIdPhotoIfAbsent asIdPhotoIfAbsent
     * @param featurePhoto featurePhoto
     * @param faceBean faceBean
     * @param removed removed
     * @param token token
     * @return FeatureBean
     */
    public <T> FeatureBean addFeatureUnchecked (T feature,String featureVersion,Integer personId,boolean asIdPhotoIfAbsent,T featurePhoto,FaceBean faceBean,String removed,Token token) {
        try{
            return addFeature(feature,featureVersion,personId,asIdPhotoIfAbsent,featurePhoto,faceBean,removed,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //3
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,java.lang.String,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param personId personId
     * @param faecBeans faecBeans
     * @param removed removed
     * @param token token
     * @return FeatureBean
     * @throws DuplicateRecordException DuplicateRecordException
     * @throws IOException IOException
     * @see IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,java.lang.String,Token)
     */    
    public <T> FeatureBean addFeature (T feature,String featureVersion,Integer personId,List<FaceBean> faecBeans,String removed,Token token) throws DuplicateRecordException,IOException{
        return addFeature(FaceUtilits.getBytesNotEmpty(feature),featureVersion,personId,faecBeans,removed,token);
    }
    //4
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,java.lang.String,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param feature feature
     * @param featureVersion featureVersion
     * @param personId personId
     * @param faecBeans faecBeans
     * @param removed removed
     * @param token token
     * @return FeatureBean
     */
    public <T> FeatureBean addFeatureUnchecked (T feature,String featureVersion,Integer personId,List<FaceBean> faecBeans,String removed,Token token) {
        try{
            return addFeature(feature,featureVersion,personId,faecBeans,removed,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //5
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,List,java.lang.String,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param personId personId
     * @param photos photos
     * @param faces faces
     * @param removed removed
     * @param token token
     * @return FeatureBean
     * @throws DuplicateRecordException DuplicateRecordException
     * @throws IOException IOException
     * @see IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,List,java.lang.String,Token)
     */    
    public <T> FeatureBean addFeature (T feature,String featureVersion,Integer personId,List<byte[]> photos,List<FaceBean> faces,String removed,Token token) throws DuplicateRecordException,IOException{
        return addFeature(FaceUtilits.getBytesNotEmpty(feature),featureVersion,personId,photos,faces,removed,token);
    }
    //6
    /**
     * {@link IFaceLog#addFeature(byte[],java.lang.String,java.lang.Integer,List,List,java.lang.String,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param feature feature
     * @param featureVersion featureVersion
     * @param personId personId
     * @param photos photos
     * @param faces faces
     * @param removed removed
     * @param token token
     * @return FeatureBean
     */
    public <T> FeatureBean addFeatureUnchecked (T feature,String featureVersion,Integer personId,List<byte[]> photos,List<FaceBean> faces,String removed,Token token) {
        try{
            return addFeature(feature,featureVersion,personId,photos,faces,removed,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //7
    /**
     * {@link IFaceLog#addImage(byte[],java.lang.Integer,FaceBean,java.lang.Integer,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param imageData imageData
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param deviceId deviceId
     * @param faceBean faceBean
     * @param personId personId
     * @param token token
     * @return ImageBean
     * @throws DuplicateRecordException DuplicateRecordException
     * @throws IOException IOException
     * @see IFaceLog#addImage(byte[],java.lang.Integer,FaceBean,java.lang.Integer,Token)
     */    
    public <T> ImageBean addImage (T imageData,Integer deviceId,FaceBean faceBean,Integer personId,Token token) throws DuplicateRecordException,IOException{
        return addImage(FaceUtilits.getBytesNotEmpty(imageData),deviceId,faceBean,personId,token);
    }
    //8
    /**
     * {@link IFaceLog#addImage(byte[],java.lang.Integer,FaceBean,java.lang.Integer,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param imageData imageData
     * @param deviceId deviceId
     * @param faceBean faceBean
     * @param personId personId
     * @param token token
     * @return ImageBean
     */
    public <T> ImageBean addImageUnchecked (T imageData,Integer deviceId,FaceBean faceBean,Integer personId,Token token) {
        try{
            return addImage(imageData,deviceId,faceBean,personId,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //9
    /**
     * {@link IFaceLog#addLog(LogBean,FaceBean,byte[],Token)}对应的泛型方法<br>
    * @param <T> T
     * @param logBean logBean
     * @param faceBean faceBean
     * @param featureImage featureImage
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param token token
     * @return void
     * @throws DuplicateRecordException DuplicateRecordException
     * @throws IOException IOException
     * @see IFaceLog#addLog(LogBean,FaceBean,byte[],Token)
     */    
    public <T> void addLog (LogBean logBean,FaceBean faceBean,T featureImage,Token token) throws DuplicateRecordException,IOException{
         addLog(logBean,faceBean,FaceUtilits.getBytesNotEmpty(featureImage),token);
    }
    //10
    /**
     * {@link IFaceLog#addLog(LogBean,FaceBean,byte[],Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param logBean logBean
     * @param faceBean faceBean
     * @param featureImage featureImage
     * @param token token
     * @return void
     */
    public <T> void addLogUnchecked (LogBean logBean,FaceBean faceBean,T featureImage,Token token) {
        try{
             addLog(logBean,faceBean,featureImage,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //11
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,byte[],FaceBean,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param featureImage featureImage
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param faceBean faceBean
     * @param token token
     * @return PersonBean
     * @throws IOException IOException
     * @see IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,byte[],FaceBean,Token)
     */    
    public <T> PersonBean savePerson (PersonBean personBean,T idPhoto,T feature,String featureVersion,T featureImage,FaceBean faceBean,Token token) throws IOException{
        return savePerson(personBean,FaceUtilits.getBytesNotEmpty(idPhoto),FaceUtilits.getBytesNotEmpty(feature),featureVersion,FaceUtilits.getBytesNotEmpty(featureImage),faceBean,token);
    }
    //12
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,byte[],FaceBean,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     * @param feature feature
     * @param featureVersion featureVersion
     * @param featureImage featureImage
     * @param faceBean faceBean
     * @param token token
     * @return PersonBean
     */
    public <T> PersonBean savePersonUnchecked (PersonBean personBean,T idPhoto,T feature,String featureVersion,T featureImage,FaceBean faceBean,Token token) {
        try{
            return savePerson(personBean,idPhoto,feature,featureVersion,featureImage,faceBean,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //13
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,List,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param photos photos
     * @param faces faces
     * @param token token
     * @return PersonBean
     * @throws IOException IOException
     * @see IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,List,Token)
     */    
    public <T> PersonBean savePerson (PersonBean personBean,T idPhoto,T feature,String featureVersion,List<byte[]> photos,List<FaceBean> faces,Token token) throws IOException{
        return savePerson(personBean,FaceUtilits.getBytesNotEmpty(idPhoto),FaceUtilits.getBytesNotEmpty(feature),featureVersion,photos,faces,token);
    }
    //14
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,List,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     * @param feature feature
     * @param featureVersion featureVersion
     * @param photos photos
     * @param faces faces
     * @param token token
     * @return PersonBean
     */
    public <T> PersonBean savePersonUnchecked (PersonBean personBean,T idPhoto,T feature,String featureVersion,List<byte[]> photos,List<FaceBean> faces,Token token) {
        try{
            return savePerson(personBean,idPhoto,feature,featureVersion,photos,faces,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //15
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param feature feature
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureVersion featureVersion
     * @param faceBeans faceBeans
     * @param token token
     * @return PersonBean
     * @throws IOException IOException
     * @see IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,Token)
     */    
    public <T> PersonBean savePerson (PersonBean personBean,T idPhoto,T feature,String featureVersion,List<FaceBean> faceBeans,Token token) throws IOException{
        return savePerson(personBean,FaceUtilits.getBytesNotEmpty(idPhoto),FaceUtilits.getBytesNotEmpty(feature),featureVersion,faceBeans,token);
    }
    //16
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],byte[],java.lang.String,List,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     * @param feature feature
     * @param featureVersion featureVersion
     * @param faceBeans faceBeans
     * @param token token
     * @return PersonBean
     */
    public <T> PersonBean savePersonUnchecked (PersonBean personBean,T idPhoto,T feature,String featureVersion,List<FaceBean> faceBeans,Token token) {
        try{
            return savePerson(personBean,idPhoto,feature,featureVersion,faceBeans,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //17
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],Token)}对应的泛型方法<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param token token
     * @return PersonBean
     * @throws IOException IOException
     * @see IFaceLog#savePerson(PersonBean,byte[],Token)
     */    
    public <T> PersonBean savePerson (PersonBean personBean,T idPhoto,Token token) throws IOException{
        return savePerson(personBean,FaceUtilits.getBytesNotEmpty(idPhoto),token);
    }
    //18
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     * @param token token
     * @return PersonBean
     */
    public <T> PersonBean savePersonUnchecked (PersonBean personBean,T idPhoto,Token token) {
        try{
            return savePerson(personBean,idPhoto,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }
    //19
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],FeatureBean,Token)}对应的泛型方法<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     *            泛型参数,参见 {@link net.gdface.utils.FaceUtilits#getBytesNotEmpty(Object)}
     * @param featureBean featureBean
     * @param token token
     * @return PersonBean
     * @throws IOException IOException
     * @see IFaceLog#savePerson(PersonBean,byte[],FeatureBean,Token)
     */    
    public <T> PersonBean savePerson (PersonBean personBean,T idPhoto,FeatureBean featureBean,Token token) throws IOException{
        return savePerson(personBean,FaceUtilits.getBytesNotEmpty(idPhoto),featureBean,token);
    }
    //20
    /**
     * {@link IFaceLog#savePerson(PersonBean,byte[],FeatureBean,Token)}泛型方法对应的unchecked方法,
     * 所有显式申明的异常都被封装到{@link RuntimeException}抛出<br>
    * @param <T> T
     * @param personBean personBean
     * @param idPhoto idPhoto
     * @param featureBean featureBean
     * @param token token
     * @return PersonBean
     */
    public <T> PersonBean savePersonUnchecked (PersonBean personBean,T idPhoto,FeatureBean featureBean,Token token) {
        try{
            return savePerson(personBean,idPhoto,featureBean,token);
        } catch(RuntimeException e){
            throw e;
        } catch(Exception e){
            throw new RuntimeException(e);
        }
    }

}